import java.awt.Color;
import java.awt.Graphics;
import java.awt.image.BufferedImage;
import java.io.*;
import javax.imageio.ImageIO;

public class PointSecteur{
	float diam;
	float rad;
	float norm;
	
	public PointSecteur()
	{
		this(10*Math.random(),7*Math.random(),100*Math.random() )
	}
	
	public PointSecteur(	float diam,	float rad,float norm)
	{
		this.diam = diam; this.rad = rad; this.norm = norm;
	}
	
		public tracer()
		{
		while rad % Math.PI*2 >rad 
	{
		 im.setRGB(i, j, Color.blue.getRGB()); // mettre le pixel (i,j) en bleu
		
	}						
	}


} 
	
